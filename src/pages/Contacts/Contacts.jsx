import './Contacts.scss';

const Contacts = () => {
    return (
        <section className='page-contacts container container_page'>
            
        </section>
    );
};

export default Contacts;